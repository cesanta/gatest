name: build
on: [push, pull_request]
jobs:
  Linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: echo hello
    - run: curl -sLO http://downloads.arduino.cc/arduino-1.8.19-linux64.tar.xz && tar -xzf *.xz && rm *.xz
